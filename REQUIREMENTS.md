# 家計簿アプリ 要件定義書

## 1. プロジェクト概要

学習用に作成する、個人利用向けのシンプルな家計簿Webアプリ。
期間(開始日〜締め日)ごとに使える金額の上限(予算)を決め、購入した物と金額を記録していくことで、
**予算内に収まっているかを一目で確認できる**ことを目的とする。

- 利用者: 自分1人のみ(ログイン不要、単一ユーザー前提)
- 目的: プログラミング学習(要件定義 → 設計 → 実装 → テストの一連の流れを経験する)

## 2. 用語定義

| 用語 | 意味 |
|---|---|
| 期間 | 予算を管理する単位。開始日と締め日で区切られる(例: 4/25〜5/24) |
| 開始日 | 期間の初日 |
| 締め日 | 期間の最終日(この日を含む) |
| 上限金額(予算) | その期間内に使ってよい金額の上限 |
| 支出 | 購入した物と金額の記録1件 |
| 使用済み金額 | 期間内の支出金額の合計 |
| 残り金額 | 上限金額 − 使用済み金額 |
| 使用率 | 使用済み金額 ÷ 上限金額 × 100(%) |

## 3. 技術方針

バックエンド・フロントエンドは Python + Django、DB は MySQL を使用する。
インフラは Terraform で AWS の EC2 と RDS を構築する。

バージョンは実際に構築した環境のもの(2026年9月時点)。未記載のものは構築時に決定し、本書に追記する。

### 3.1 アプリケーション

| 区分 | 技術 | バージョン | 備考 |
|---|---|---|---|
| 言語 | Python | 3.13 | バックエンド・画面生成ともにPythonで記述する |
| フレームワーク | Django | 5.2(LTS) | 画面・入力チェック・DBアクセスをDjangoの標準機能で行う。長期サポート版を採用 |
| 画面生成(フロントエンド) | Djangoテンプレート + HTML/CSS | (Djangoに準拠) | サーバー側でHTMLを生成して返す(SPA・REST APIは使わない) |
| クライアント側の処理 | JavaScript(ライブラリなし) | (ブラウザ標準) | 削除時の確認ダイアログなど最小限に留める |
| 入力チェック | Django Forms | (Djangoに準拠) | F-01〜F-05 の入力値チェックを行う |
| DBアクセス | Django ORM | (Djangoに準拠) | パラメータ化クエリが標準で適用され、SQLインジェクション対策になる |
| DB | MySQL | 8.4(LTS) | 本番は Amazon RDS for MySQL。ローカル開発環境も Docker Compose(`docker-compose.yml`)で用意した MySQL を使い、本番とそろえる。文字コードは utf8mb4。Strict Mode(不正な値を切り詰めずにエラーにする設定)を接続時に有効にする |
| DBドライバー | mysqlclient | 2.3.0 | Djangoが推奨するMySQL用ドライバー |
| 設定値の読み込み | python-dotenv | 1.2.3 | SECRET_KEY・DB接続情報などを `.env` から読み込む |
| アプリケーションサーバー | Gunicorn | 26.2.0 | EC2上でDjangoを動かす |
| Webサーバー | Nginx | (Ubuntu 24.04 の標準パッケージ) | EC2上でリクエストを受け、Gunicornへ転送する。CSSなどの静的ファイルも配信する |
| テスト | Django標準のテスト機能 | (Djangoに準拠) | 使用率の計算・警告状態の判定・入力チェックを中心にテストする |

※F-07 の背景色の状態(通常/警告/超過)はサーバー側で判定し、画面の `body` 要素に状態を表すクラスを付けてCSSで色を切り替える。

### 3.2 インフラ

| 区分 | 技術 | 備考 |
|---|---|---|
| クラウド | AWS(東京リージョン ap-northeast-1) | 無料プランのクレジットの範囲内で利用する |
| IaC | Terraform | AWSリソースはすべてTerraformで作成・削除する(`infra/`) |
| アプリ実行環境 | Amazon EC2(t4g.micro、Ubuntu Server 24.04 LTS) | パブリックサブネットに配置し、Elastic IP を付ける(停止・起動してもアドレスが変わらないようにするため) |
| DB | Amazon RDS for MySQL(db.t4g.micro、MySQL 8.4、ストレージ 20GB) | プライベートサブネットに配置し、**EC2からのみ接続できる**ようにする。シングルAZ、自動バックアップは1日分 |
| 秘密情報の保管 | AWS Systems Manager パラメータストア | DBのパスワードをTerraformでランダムに生成し、暗号化(SecureString)して保存する。EC2は起動時にここから読み取る |
| サーバーへの接続 | AWS Systems Manager セッションマネージャー | SSHの代わりに使う。ポート22を開けず、鍵ファイルの管理も不要になる |
| デプロイ | EC2の初回起動時に実行するスクリプト(ユーザーデータ) | GitHubの `main` ブランチを取得し、Python・Nginx・Gunicornのインストールからアプリの起動までを自動で行う |

※インスタンスの種類は、無料プランの対象の中から、Arm版で料金が最も安いものを選んでいる。費用の目安は月 約33 USD(EC2 約8、RDS 約18、パブリックIPv4 約4、ストレージ 約3.5)で、無料プランのクレジットから差し引かれる。

構成イメージ:

```
インターネット
   │  HTTP(80) ※自分のIPアドレスからのみ許可
   ▼
┌─ VPC ─────────────────────────────────────────┐
│  パブリックサブネット                          │
│   EC2(Nginx + Gunicorn + Django)              │
│      │  MySQL(3306)                           │
│      ▼  ※EC2のセキュリティグループからのみ許可 │
│  プライベートサブネット(2つのAZ)              │
│   RDS for MySQL                                │
└────────────────────────────────────────────────┘
```

### 3.3 インフラのセキュリティ要件

| 対象 | 要件 |
|---|---|
| RDS | インターネットからの接続を受け付けない(パブリックアクセスを無効にする) |
| RDS | プライベートサブネットに配置する(RDSの仕様上、2つのAZにサブネットを用意する) |
| RDS | セキュリティグループの受信ルールは、ポート3306を **EC2のセキュリティグループからのみ** 許可する |
| EC2 | アプリはログイン機能を持たないため、HTTP(80)は **自分のIPアドレスからのみ** 許可する |
| EC2 | SSH(22)は開けない。サーバーへの管理用の接続は SSM セッションマネージャーで行う |
| EC2 | インスタンスメタデータは IMDSv2(トークン必須)のみ許可する。ディスクは暗号化する |
| IAM | EC2に付けるロールの権限は、SSMでの接続と、DBパスワードのパラメータの読み取りのみに限定する |
| 秘密情報 | DBのパスワードなどはGitに含めない(Terraformの変数ファイル・状態ファイル、Djangoの `.env` は `.gitignore` で除外する) |
| 運用 | `terraform apply` / `destroy` は、必ず `terraform plan` の結果を確認してから実行する |
| 費用 | 無料利用枠の対象となる小さいインスタンスを使い、使わないときは `terraform destroy` で削除する |

## 4. 必須機能(MUST)

| No | 機能 | 内容 |
|---|---|---|
| F-01 | 期間の設定 | 開始日と締め日を設定して期間を作成・編集する。締め日は開始日以降であること |
| F-02 | 上限金額の設定 | 期間ごとに上限金額(予算)を設定・変更する |
| F-03 | 支出の登録 | 購入した物(品名)と金額を同時に入力して登録する。購入日も記録する |
| F-04 | 支出の一覧表示 | 選択中の期間内の支出を一覧表示する(購入日の新しい順) |
| F-05 | 支出の編集・削除 | 登録済みの支出の品名・金額・購入日を修正、または削除する |
| F-06 | 予算状況の表示 | 上限金額・使用済み金額・残り金額・使用率を表示する。使用率はプログレスバーでも表示する |
| F-07 | 上限接近時の警告表示 | 使用率に応じて画面の背景色を変え、上限に近づいたことを一目でわかるようにする(下表) |

### F-07 警告表示の段階

| 状態 | 背景色 | 条件(初期値) |
|---|---|---|
| 通常 | 白(通常色) | 使用率 80% 未満 |
| 警告 | 赤(薄い赤) | 使用率 80% 以上 100% 以下 |
| 超過 | 赤(濃い赤) | 使用率 100% 超(上限金額を超えた)。超過額も表示する |

※警告のしきい値(80%)は定数として1か所にまとめ、後から変更しやすくする。

## 5. あったらいい機能(WANT)

| No | 機能 | 内容 |
|---|---|---|
| W-01 | 過去の期間の閲覧 | 過去の期間を切り替えて、当時の支出と予算状況を確認する |
| W-02 | 次の期間の自動作成 | 前の期間と同じ日付の区切り・上限金額で次の期間を作成する |
| W-03 | 警告しきい値の変更 | 警告に切り替わる使用率(初期値 80%)を画面から変更する |
| W-04 | カテゴリ分け | 支出に「食費」「日用品」などのカテゴリを付け、カテゴリ別の合計を表示する |
| W-05 | 1日あたりの目安 | 残り金額 ÷ 締め日までの残り日数 で、1日に使える目安金額を表示する |

## 6. データ項目

実装は [budget/models.py](budget/models.py) を参照。

```
期間(Period) 1 ──── * 支出(Expense)
```

- 支出は、登録時に選択していた期間に紐付けて保存する(外部キー)。購入日がその期間内であることを入力チェックで保証するため、「どの期間に属するかは購入日で判定する」という要件と一致する
- 支出が1件でも残っている期間は削除できない(期間の削除機能は今回は作らないが、データの不整合を防ぐため)
- 入力チェックはアプリ側(Djangoのモデル・フォーム)で行い、締め日 ≧ 開始日・金額 ≧ 1円などの基本的な制約はDB側にも設定する

### 期間(Period)

| 項目 | 必須 | 制約 |
|---|---|---|
| 開始日 | ○ | 日付 |
| 締め日 | ○ | 日付。開始日以降 |
| 上限金額 | ○ | 1円以上の整数 |

※期間同士の日付は重複しないこと。

※期間の開始日・締め日を変更したとき、登録済みの支出が期間外になる場合は保存できない(エラーメッセージを表示し、先に該当する支出の修正・削除を促す)。

### 支出(Expense)

| 項目 | 必須 | 制約 |
|---|---|---|
| 期間 | ○ | 所属する期間 |
| 品名 | ○ | 1〜50文字。前後の空白は取り除き、空白のみは未入力として扱う |
| 金額 | ○ | 1円以上の整数 |
| 購入日 | ○ | 日付。選択中の期間内(開始日〜締め日)の日付に限る。初期値は当日(当日が期間外の場合は期間の開始日)。どの期間に属するかは購入日で判定する |

## 7. 画面構成(概要)

1画面(メイン画面)を基本とし、期間の設定のみ別画面とする。

| 画面 | URL | 内容 |
|---|---|---|
| トップ | `/` | 今日を含む期間(なければ開始日が最も新しい期間)のメイン画面へ移動する。期間が1つもなければ期間の設定を促す |
| メイン画面 | `/periods/<id>/` | 下記の3つのエリアを表示する |
| 支出の編集 | `/expenses/<id>/edit/` | メイン画面の支出入力エリアを編集モードにして表示する |
| 期間の作成 | `/periods/new/` | 開始日・締め日・上限金額の入力フォーム |
| 期間の編集 | `/periods/<id>/edit/` | 同上(現在の値を表示) |

メイン画面の構成:

- **ヘッダー**: 期間の切り替え(プルダウン)、期間の編集・新しい期間の作成へのリンク
- **予算状況エリア**: 期間(開始日〜締め日)、上限金額、使用済み金額、残り金額(超過時は超過額)、使用率(プログレスバー)、状態(予算内/上限に近づいています/上限を超えました)
- **支出入力エリア**: 品名・金額・購入日の入力欄と登録ボタン
- **支出一覧エリア**: 支出の一覧(購入日・品名・金額)と編集・削除ボタン。削除時は確認ダイアログを表示する
- 画面全体の背景色は F-07 の状態に応じて切り替わる
- 使用率は小数点以下を切り捨てて表示する(例: 79.99% は「79%」。背景色の判定と表示が食い違わないようにするため)
- 登録・更新・削除が完了したら、画面上部に完了メッセージを表示する

## 8. 非機能要件

- 金額は円単位の整数で扱い、表示時は3桁区切り(例: ¥12,345)にする
- 入力値が不正な場合(未入力・0円以下・文字数超過など)は登録せず、エラーメッセージを表示する
- 動作環境: PCのブラウザ(Google Chrome 最新版)
- 公開環境: AWS(EC2 + RDS)。構成とセキュリティ要件は「3. 技術方針」を参照

## 9. スコープ外(今回は作らない)

- ログイン・複数ユーザー対応
- 収入の管理
- 銀行口座・クレジットカードとの連携
- スマホ対応(余裕があれば後日)
- グラフによる分析機能
- HTTPS化・独自ドメインの設定
- 冗長構成(RDSのマルチAZ配置など)・自動バックアップの設計
