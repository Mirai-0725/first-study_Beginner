# 家計簿アプリ 要件定義書

## 1. プロジェクト概要

学習用に作成する、個人利用向けのシンプルな家計簿Webアプリ。
期間(開始日〜締め日)ごとに使える金額の上限(予算)を決め、購入した物と金額を記録していくことで、
**予算内に収まっているかを一目で確認できる**ことを目的とする。

- 利用者: 自分1人のみ(ログイン不要、単一ユーザー前提)
- 目的: プログラミング学習(要件定義 → 設計 → 実装 → テストの一連の流れを経験する)

## 2. 用語定義

| 用語 | 意味 |
|---|---|
| 期間 | 予算を管理する単位。開始日と締め日で区切られる(例: 4/25〜5/24) |
| 開始日 | 期間の初日 |
| 締め日 | 期間の最終日(この日を含む) |
| 上限金額(予算) | その期間内に使ってよい金額の上限 |
| 支出 | 購入した物と金額の記録1件 |
| 使用済み金額 | 期間内の支出金額の合計 |
| 残り金額 | 上限金額 − 使用済み金額 |
| 使用率 | 使用済み金額 ÷ 上限金額 × 100(%) |

## 3. 技術方針

**未決定**(実装に着手する前に決定し、本書を更新する)。

検討候補:

| 案 | 構成 | 特徴 |
|---|---|---|
| A | React (Vite) のみ + ブラウザの localStorage に保存 | サーバー不要で最もシンプル。フロントエンドの学習に集中できる |
| B | React (Vite) + Java/Spring Boot + PostgreSQL | フロント・バック・DBを一通り学べる。構成は大きくなる |

## 4. 必須機能(MUST)

| No | 機能 | 内容 |
|---|---|---|
| F-01 | 期間の設定 | 開始日と締め日を設定して期間を作成する。締め日は開始日以降であること |
| F-02 | 上限金額の設定 | 期間ごとに上限金額(予算)を設定・変更する |
| F-03 | 支出の登録 | 購入した物(品名)と金額を同時に入力して登録する。購入日も記録する |
| F-04 | 支出の一覧表示 | 選択中の期間内の支出を一覧表示する(購入日の新しい順) |
| F-05 | 支出の編集・削除 | 登録済みの支出の品名・金額・購入日を修正、または削除する |
| F-06 | 予算状況の表示 | 上限金額・使用済み金額・残り金額・使用率を表示する。使用率はプログレスバーでも表示する |
| F-07 | 上限接近時の警告表示 | 使用率に応じて画面の背景色を変え、上限に近づいたことを一目でわかるようにする(下表) |

### F-07 警告表示の段階

| 状態 | 背景色 | 条件(初期値) |
|---|---|---|
| 通常 | 白(通常色) | 使用率 80% 未満 |
| 警告 | 赤(薄い赤) | 使用率 80% 以上 100% 以下 |
| 超過 | 赤(濃い赤) | 使用率 100% 超(上限金額を超えた)。超過額も表示する |

※警告のしきい値(80%)は定数として1か所にまとめ、後から変更しやすくする。

## 5. あったらいい機能(WANT)

| No | 機能 | 内容 |
|---|---|---|
| W-01 | 過去の期間の閲覧 | 過去の期間を切り替えて、当時の支出と予算状況を確認する |
| W-02 | 次の期間の自動作成 | 前の期間と同じ日付の区切り・上限金額で次の期間を作成する |
| W-03 | 警告しきい値の変更 | 警告に切り替わる使用率(初期値 80%)を画面から変更する |
| W-04 | カテゴリ分け | 支出に「食費」「日用品」などのカテゴリを付け、カテゴリ別の合計を表示する |
| W-05 | 1日あたりの目安 | 残り金額 ÷ 締め日までの残り日数 で、1日に使える目安金額を表示する |

## 6. データ項目

### 期間

| 項目 | 必須 | 制約 |
|---|---|---|
| 開始日 | ○ | 日付 |
| 締め日 | ○ | 日付。開始日以降 |
| 上限金額 | ○ | 1円以上の整数 |

※期間同士の日付は重複しないこと。

### 支出

| 項目 | 必須 | 制約 |
|---|---|---|
| 品名 | ○ | 1〜50文字 |
| 金額 | ○ | 1円以上の整数 |
| 購入日 | ○ | 日付。初期値は当日。どの期間に属するかは購入日で判定する |

## 7. 画面構成(概要)

1画面(メイン画面)を基本とする。

- **予算状況エリア**: 期間(開始日〜締め日)、上限金額、使用済み金額、残り金額、使用率(プログレスバー)
- **支出入力エリア**: 品名・金額・購入日の入力欄と登録ボタン
- **支出一覧エリア**: 支出の一覧(品名・金額・購入日)と編集・削除ボタン
- **期間設定**: 開始日・締め日・上限金額を設定するフォーム(モーダルまたは別画面)
- 画面全体の背景色は F-07 の状態に応じて切り替わる

## 8. 非機能要件

- 金額は円単位の整数で扱い、表示時は3桁区切り(例: ¥12,345)にする
- 入力値が不正な場合(未入力・0円以下・文字数超過など)は登録せず、エラーメッセージを表示する
- 動作環境: PCのブラウザ(Google Chrome 最新版)

## 9. スコープ外(今回は作らない)

- ログイン・複数ユーザー対応
- 収入の管理
- 銀行口座・クレジットカードとの連携
- スマホ対応(余裕があれば後日)
- グラフによる分析機能
